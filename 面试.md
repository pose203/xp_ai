**三栏布局的实现方法**
三栏布局是前端开发中非常经典的布局方式，通常指的是左右两栏固定宽度，中间一栏自适应的布局。我来为您介绍几种主要的实现方法：
1. Float 浮动布局（传统方法，兼容性好）
2. Position 绝对定位布局
3. Flexbox 弹性布局（现代推荐）
4. CSS Grid 网格布局（最新方法）


**prototype和__proto__的区别**
 - prototype 属于函数，__proto__ 属于对象实例。
 - prototype是构造函数的“蓝图”，用于实例继承，__proto__是实例的“原型指针”，构成原型链
 - 实例的 __proto__ 指向其构造函数的 prototype
 - 当一个函数被用作构造函数创建实例时，该实例的 __proto__ 属性会指向该构造函数的 prototype 属性所指向的对象。

**手写new的过程**
```js
function myNew(constructor, ...args) {
    // 1. 创建一个全新的空对象
    const obj = {};
    // 2. 将新对象的原型 (__proto__) 指向构造函数的 prototype
    // 使用 Object.create() 是最标准的方式，它创建一个新对象，其原型是传入的对象
    Object.setPrototypeOf(obj, constructor.prototype);
    // 3. 将构造函数的 this 绑定到新对象 obj 上，并执行构造函数，传入参数
    const result = constructor.apply(obj, args);
    // 4. 返回新对象
    if (result !== null && (typeof result === 'object' || typeof result === 'function')) {
        return result;
    }
    return obj;
}
```
**手写promise的过程**
- 搭建基础结构和状态管理
- 实现 then 方法（同步情况）
- 处理异步情况（存储回调）
- 实现链式调用（返回新的 Promise）

**React hooks**
- 内置的hooks
    - useState, useEffect(副作用)、
    - useContext 
    - useReducer 用于更复杂的状态逻辑管理
    - useRef 用于创建一个可变的引用对象
    - useLayoutEffect 是 React Hooks 中的一个函数，它在 DOM 更新后、浏览器绘制前同步执行，适合用于需要读取 DOM 布局并同步更新的场景，以避免视觉闪烁。
    - useMemo 用于缓存计算结果
    - useCallback 用于缓存回调函数
- 自定义的hooks
    - useTitle,useTodos,useMouse, useRepos 
    - 响应式业务、响应式场景封装到hooks/目录下， 复用
    - UI组件干净
- ahooks 第三方hooks/vueuse 库 

**防抖节流**
- 防抖：在事件被触发n秒后才执行，如果在这n秒内事件又被触发，则重新计时
    - 主要应用于搜索框和按钮
- 节流：在规定时间内只执行一次
    - 定时器和时间戳
    - 主要应用于滚动事件中的监听
    - 鼠标移动

**你对react有什么理解**
- React 是一个用于构建用户界面的 JavaScript 库，核心思想是组件化开发和数据驱动视图。
- 组件化：将页面拆分为独立的组件，每个组件可复用、易维护，比如一个按钮组件可在多个页面重复使用。
- 虚拟DOM：通过内存中的虚拟DOM树对比差异，最小化真实DOM操作，大幅提升渲染效率。
- 单向数据流：数据从父组件流向子组件（通过props），状态集中管理（如配合Redux），避免数据混乱。
- React还支持服务端渲染（Next.js）、跨平台开发（React Native），生态丰富，适合大型项目协作。

**请问react的子父通信 兄弟通信如何实现**
- 子组件通过 props 接收父组件传入的函数（回调函数），触发时将数据传递给父组件。（单向数据流）
- 子父组件通信（子 → 父）
    - 原理：父组件传递一个回调函数给子组件，子组件通过调用该回调函数将数据传递回父组件。
- 兄弟通信：
    - 状态提升：将共享状态交给共同的父组件管理，通过props传递给两个子组件。
    - Context API：创建共享上下文，避免逐层传递props。
    - 第三方库：如Redux或MobX进行全局状态管理。

**讲讲js的特点**
- JavaScript 是一门动态类型、弱类型、基于原型链的语言，特点包括：
    - 动态类型：变量无需声明类型，运行时自动推断（如 let a = 1; a = "str"）。
    - 事件驱动：以事件循环（Event Loop）为核心，擅长处理异步任务（如点击事件、AJAX请求）。
    - 函数式编程：支持高阶函数、闭包、匿名函数，常用于回调和链式调用。
    - 跨平台：通过Node.js可开发后端，结合Electron可开发桌面应用。
    - 灵活性与缺陷：语法灵活但易写“脆弱代码”，需配合TypeScript或严格规范（ESLint）提升健壮性。

**es6有哪些新特性**
- 块级作用域：let/const 替代 var，避免变量污染。
- 箭头函数：简化函数写法，自动绑定 this。（它的 this 是从定义时的外层作用域 继承 而来的。这种特性被称为 词法作用域绑定（Lexical this Binding），即 this 的值在函数定义时就已经确定，而不是在调用时动态绑定。）
- 模板字符串：用反引号（`）拼接字符串和变量。
- 解构赋值：快速提取数组元素或对象属性。
- 模块化：import/export 替代CommonJS的 require。
- 类（Class）：提供更直观的面向对象语法（class Person {}）。
- Promise：原生支持异步操作，替代回调地狱。

**讲讲js有哪些异步任务**
- 定时器：setTimeout和setInterval用于延迟或周期性执行代码。
- 事件处理：如用户交互事件（点击、输入）或浏览器事件（加载、网络请求）。
- AJAX请求：通过XMLHttpRequest或Fetch API异步获取数据。
- Promise和Async/Await：用于管理异步操作，避免回调地狱。
- Web Workers：在后台线程执行脚本，不阻塞主线程。
- 微任务（Microtasks）：如Promise.then、MutationObserver，在宏任务结束后立即执行。

**说说索引有什么作用**
- 加速查询：通过B+树或哈希结构快速定位数据，避免全表扫描。
- 保证唯一性序与分组优：如主键索引强制唯一性约束。
- 排化：索引字段已排序，可加速 ORDER BY 和 GROUP BY 操作。

**索引是越多越好吗**
- 索引不是越多越好：
    - 写入性能下降：每次插入/更新数据时，索引需要同步更新，增加开销。 。、
    - 占用存储空间：每个索引都需要额外存储。
    - 选择性低的索引无效：如性别字段（男/女）建索引效果差，因为选择性低。

**对AI的理解**
- AI是通过算法模拟人类智能行为的技术，核心包括机器学习、深度学习和自然语言处理等。比如，AI能通过大量数据训练模型，自动完成图像识别、语音助手、推荐系统等任务。它正在改变各行业——医疗领域辅助诊断、金融风控、自动驾驶等。但AI也面临挑战，比如数据隐私、算法偏见和伦理问题。我认为未来AI会更注重“人机协作”，成为提升效率的工具，而非完全替代人类。

**react的优势是什么**
- 组件化开发：将界面拆分为独立组件，提高复用性和维护性，比如一个按钮组件可复用到多个页面。
- 虚拟DOM：通过对比新旧DOM差异，最小化真实DOM操作，显著提升性能。
- 生态强大：配合Redux管理状态、React Router实现路由，且社区活跃，资源丰富。另外，单向数据流和JSX语法让开发更直观，尤其适合大型项目协作。

**了解ts吗**
- TypeScript 是 JavaScript 的超集，核心是静态类型检查。比如定义变量时指定类型，编译时就能发现错误，减少运行时BUG。它还支持接口、泛型等高级特性，提升代码可读性和团队协作效率。在React项目中，TS能强制组件props类型，避免传参错误。虽然初期学习成本略高，但长期看能降低维护成本。我之前在项目中用TS重构代码，确实减少了很多类型相关BUG。

**了解Electron吗**
- Electron 基于Chromium和Node.js，允许用HTML/CSS/JS开发跨平台桌面应用，比如VS Code、Slack。它的优势是开发效率高，前端开发者可快速上手，且能调用系统API（如文件操作）。但缺点是应用体积较大，性能不如原生应用。我曾用Electron做一个跨平台的本地日志分析工具，通过IPC通信实现主进程与渲染进程的数据交互，体验还不错。不过对性能要求高的场景，比如音视频处理，可能更适合原生开发。

**虚拟DOM和diff算法**
- 虚拟DOM：通过JS对象模拟DOM树，对比新旧DOM差异，最小化真实DOM操作，提升渲染性能。
    - 提升性能：通过在内存中维护轻量级的虚拟 DOM 树，减少直接操作真实 DOM 的次数，避免频繁的重排（Reflow）和重绘（Repaint）。
    - 跨平台能力：虚拟 DOM 是框架层的抽象，使得 React 可以渲染到 Web、Native（React Native）、Canvas 等不同平台。
    缺点:
        - 额外的内存开销：需要在内存中维护两棵 VDOM 树（旧的和新的）。
- diff算法：通过深度优先遍历，找到新旧DOM差异，仅更新差异部分，最小化操作，提高渲染性能。
    - 同层节点比较: 只对同一层级的节点进行比较，不跨层级移动。
    - 类型不同的节点产生不同的树: 如果节点类型不同，React 会直接销毁旧节点，创建新节点。
    - 通过key标识节点: 使用 key 属性标识节点，React 会根据 key 判断节点是否需要更新。
    缺点:
        - 额外的 JavaScript 计算：Diff 算法本身需要执行 JavaScript 计算。
       

**==和===的区别**
- `==` **等于**：会进行类型转换再比较。如果两边的类型不同，会尝试转换成相同类型再进行比较。
- `===` **严格等于**：不会进行类型转换，直接比较。如果类型不同，直接返回 `false`。
- **总结**：为了代码的严谨性和可预测性，**推荐始终使用 `===`**。
- **示例**：`'1' == 1` 结果是 `true`，因为字符串 `'1'` 被转换成了数字 `1`。而 `'1' === 1` 结果是 `false`。


**css选择器有哪些**

- 基础选择器
   - 元素选择器:选择文档中所有指定类型的 HTML 元素
   - 类选择器:选择具有指定 class 属性值的元素。
   - ID选择器:选择具有指定 id 属性值的唯一元素
   - 通用选择器:选择文档中的所有元素
- 组合选择器
   - 后代选择器:选择指定元素的后代元素
   - 子元素选择器:选择指定元素的直接子元素
   - 相邻兄弟选择器:选择指定元素的紧邻的下一个兄弟元素
   - 通用兄弟选择器:选择指定元素的后续所有兄弟元素
- 伪类选择器
  - 用于描述元素的特殊状态或位置。
  - 行为伪类
  - 状态伪类
  - 结构伪类
  - 条件伪类
- 伪元素选择器
  - 用于选择元素的特定部分（通常是 CSS 生成的内容），而不是整个元素
- 属性选择器
  - 根据元素的属性或属性值来选择元素。

**position有哪些属性**
- static 默认值，元素按照正常文档流进行定位。
- relative 相对定位，相对于自身的位置定位
- absolute 绝对定位，相对于离他最近的(管着它的)position 不为static 的属性定位
- fixed 固定定位，相对于浏览器窗口进行定位
- sticky 粘性定位，根据滚动位置进行定位

**display有哪些属性**
- `block`：块级元素。会独占一行，可以设置宽高、内外边距。
- `inline`：行内元素。不会独占一行，和其他行内元素并排。`width` 和 `height` 设置无效，`margin` 的上下外边距无效，但 `padding` 的上下内边距有效但不会撑开布局空间。
- `inline-block`：行内块级元素。表现为行内元素（不独占一行），但内部表现为块级元素（可以设置宽高、内外边距）。
- `flex`：弹性布局。容器变为 Flex 容器，用于创建一维布局。
- `grid`：网格布局。容器变为 Grid 容器，用于创建二维布局。
- `none`：元素不会被显示，且不占据任何空间。

**发布订阅模式**
- 发布订阅模式是一种设计模式，它定义了一种一对多的依赖关系，让多个订阅者对象同时监听某一个主题对象。这个主题对象在自身状态变化时，会通知所有订阅者对象，使它们能够自动更新。
- 发布订阅模式有三个角色：
    - 发布者（Publisher）：发布者是事件的触发者，负责发布事件。
    - 订阅者（Subscriber）：订阅者是事件的接收者，负责接收事件。
    - 事件中心（Event Center）：事件中心是发布者和订阅者之间的桥梁，负责管理事件的发布和订阅。
- 实现步骤：
    - 订阅者:   
       - 订阅者 A 向事件中心注册，说：“我想监听 userLoggedIn 事件，当它发生时，请调用我的 handleLogin 函数。”
       - 订阅者 B 向事件中心注册，说：“我想监听 userLoggedIn 事件，当它发生时，请调用我的 updateHeader 函数。”
    - 发布 (Publish)：
       - 当用户成功登录后，登录模块（发布者）向事件中心发布一个 userLoggedIn 事件（可能携带用户数据）。
    - 通知 (Notify)：
       - 事件中心收到 userLoggedIn 事件后，查找所有订阅了该事件的回调函数。
       - 它依次调用 handleLogin 和 updateHeader，并将事件数据作为参数传递。
    - 执行：
       - 订阅者 A 和 B 的回调函数被执行，各自完成自己的逻辑（如加载用户数据、更新 UI 等）。
- 发布订阅模式的好处：
    - 松耦合：发布者和订阅者之间没有直接的依赖关系，可以独立变化。
    - 可扩展性：可以动态添加和删除订阅者。
    - 可复用性：可以复用发布者和订阅者。

**类组件和函数组件的区别**
- **语法与定义**：
  - **类组件**：基于 ES6 Class 语法，需要继承 `React.Component`，通过 `render()` 方法返回 JSX。
  - **函数组件**：就是一个普通的 JavaScript 函数，接收 `props` 作为参数，返回 JSX。
- **状态管理**：
  - **类组件**：通过 `this.state` 定义和管理状态，使用 `this.setState()` 更新状态。`setState` 更新是异步的且可能会被合并。
  - **函数组件**：通过 `useState` Hook 来管理状态。更新状态的函数（如 `setCount`）可以直接调用。
- **生命周期**：
  - **类组件**：拥有一套完整的生命周期钩子函数，如 `componentDidMount`, `componentDidUpdate`, `componentWillUnmount` 等。
  - **函数组件**：使用 `useEffect` Hook 来模拟生命周期行为，可以处理组件挂载、更新、卸载等副作用。
- **`this` 指向**：
  - **类组件**：内部有 `this` 指向，在事件处理函数中需要注意 `this` 的绑定问题。
  - **函数组件**：没有 `this` 的概念，避免了 `this` 指向混乱的问题。
- **逻辑复用**：
  - **类组件**：主要通过高阶组件（HOC）和 Render Props 来复用逻辑。
  - **函数组件**：通过自定义 Hook 来复用状态逻辑，更加简洁和灵活。
- **发展趋势**：
  - 自 React 16.8 推出 Hooks 以来，函数组件已成为社区主流和官方推荐的组件编写方式，因为它更简洁、易于测试和理解。

**浏览器原生监视视窗的方法**
- 核心原生 API:Intersection Observer API
  - 图片/视频懒加载：当图片滚动到视口附近时再加载真实资源。
  - 无限滚动：当滚动到页面底部的“哨兵”元素时，加载下一页数据。
  - 视差滚动/动画：当元素进入视口时触发动画效果。
  - 内容可见性统计：统计用户实际看到了哪些内容。
- scroll 事件 + getBoundingClientRect()
- ResizeObserver API

**组件的封装**
- 目的：
    - 提升开发效率：复用已封装的组件，避免重复代码。
    - 维护性：将复杂逻辑拆分为小模块，降低维护成本。
    - 一致性：统一 UI 风格和交互逻辑，符合设计规范。
- 组件分类：
    - 功能组件：纯逻辑处理（如表单验证、请求封装）。
    - UI 组件：基础视图元素（如按钮、输入框）。
    - 业务组件：结合业务逻辑的复杂组件（如用户登录面板）。
    - 高阶组件（HOC）：通过组合逻辑增强组件功能。
- 封装原则：
    - 单一职责：每个组件只负责一个功能。
    - 接口明确：通过 props 和事件传递数据，解耦内外部逻辑。
    - 可扩展性：预留扩展点，适应未来需求变化

**websocket有没有了解**
- 基于 TCP 的双向通信（客户端 ↔ 服务器）。
- 实时性更高，适合高频交互（如在线游戏、聊天室）。
- 需要手动管理连接和重连逻辑。

**http和https的区别**
- 安全性：HTTPS 使用 SSL/TLS 加密通信，更安全。
- 端口：HTTP 使用 80 端口，HTTPS 使用 443 端口。
- 证书：HTTPS 需要 SSL 证书，HTTP 不需要。
- 性能：HTTPS 比 HTTP 更慢，因为需要加密和解密。


**api 请求方式是怎么样的**
- XMLHttpRequest（XHR）
    - 传统方式，支持异步请求，但语法较复杂。
    - 适用于旧项目或需要兼容低版本浏览器的场景。
- Fetch API
    - 现代标准，基于 Promise，语法简洁。
    - 支持流式响应（如 response.body.getReader()）。
- Beacon API
    - 用于页面卸载时发送小数据（如埋点统计）。
    - 保证数据发送成功，即使页面关闭。
- SSE（Server-Sent Events）
    - 用于服务器主动推送文本数据（如 AI 流式输出）。
    - 示例：const eventSource = new EventSource('/stream')。
- WebSocket
    - 用于双向实时通信（如聊天室）。
    - 示例：const socket = new WebSocket('wss://example.com')。

**flex 布局哪些常用属性**
Flex 布局通过 display: flex 实现灵活的布局，常用属性包括：
- 容器属性
    - flex-direction：主轴方向（row / column）。
    - justify-content：主轴对齐方式（flex-start / center / space-between）。
    - align-items：交叉轴对齐方式（flex-start / stretch）。
    - flex-wrap：是否换行（nowrap / wrap）。
- 项目属性
    - flex-grow：扩展比例（如 flex-grow: 1 填满剩余空间）。
    - flex-shrink：收缩比例（默认 1，可压缩）。
    - align-self：覆盖容器的 align-items（如 center）。

- **zustand的实现原理**
  - create 函数：创建一个包含状态和更新方法的 Store，并返回一个自定义 Hook (useStore)。
  - set / get：提供更新和读取状态的 API。
  - useStore(selector)：组件通过此 Hook 订阅 Store 中特定的部分。
  - 发布-订阅 + 精细比较：核心机制。状态更新时，遍历所有订阅者，通过 selector 重新计算其关注的状态，并与旧值进行浅比较。仅当值变化时才触发组件重渲染，实现了高效更新。
  - 不可变更新：确保状态引用变化，使浅比较有效。

**react 的性能优化**
- **减少不必要的重新渲染**：
  - **组件层级**：使用 `React.memo` 缓存函数组件，或 `PureComponent` 缓存类组件，当 `props` 未改变时跳过渲染。
  - **值和函数**：使用 `useMemo` 缓存昂贵的计算结果，使用 `useCallback` 缓存函数引用，避免作为 `props` 传递时导致子组件不必要的重渲染。
  - **依赖数组**：精确设置 `useEffect`、`useMemo`、`useCallback` 的依赖数组，避免不必要的执行。
- **列表性能优化**：
  - **`key` 属性**：为列表中的每一项提供稳定且唯一的 `key` 属性，帮助 React 识别项目，优化 diff 算法。
  - **虚拟化/窗口化**：对于长列表，使用 `react-window` 或 `react-virtualized` 等库，只渲染可视区域内的列表项，极大地提升性能。
- **代码分割 (Code Splitting)**：
  - 使用 `React.lazy` 和 `Suspense` 来实现组件的动态导入，按需加载代码，减小首屏加载体积。

**useEffect,useState**
- useState
    - 用途：管理组件的内部状态，当状态变化时触发组件重新渲染。
- useEffect
    - 用途：处理副作用（如数据获取、订阅、手动操作 DOM），替代类组件中的 componentDidMount、componentDidUpdate、componentWillUnmount。
    - 依赖数组：
        - 若依赖项为空数组 []，副作用仅在组件挂载和卸载时执行。
        - 若依赖项为 state 或 props，当它们变化时副作用重新执行。

**闭包**
- 闭包是指函数能够访问并记住其词法作用域，即使该函数在其作用域外执行。
- 内部函数引用外部函数的变量，即使外部函数已经执行完毕，变量也不会被释放。
- 优点
    - 封装私有变量：避免全局污染。
    - 持久化数据：变量长期存储在内存中（如计数器、缓存）。
- 缺点
    - 内存泄漏风险：若闭包引用了大对象且未及时释放，可能导致内存占用过高。


**浏览器页面渲染的过程**
- 解析 HTML 和 CSS
    - HTML 解析：生成 DOM 树。
    - CSS解析：生成 CSSOM 树。
- 关键阻塞：浏览器会暂停 HTML 解析等待 CSS 加载（避免 FOUC）。
- 生成渲染树（Render Tree）
    - 将 DOM 树和 CSSOM 树合并为 渲染树（忽略 display: none 的元素）。
- 布局（Layout）
    - 计算每个节点的几何位置（宽度、高度、偏移量）。
- 触发条件：修改 DOM 结构、样式（如 width、height）。
- 绘制（Paint）
    - 将渲染树的每个节点转换为像素（颜色、文本、边框等）。
- 合成（Composite）
    - 将多个图层（Layer）合成为最终的像素输出到屏幕。

**浅拷贝和深拷贝**
- 浅拷贝
  - 浅拷贝是创建一个新对象，这个对象有着原始对象属性值的一份精确拷贝。如果属性是基本类型，拷贝的就是基本类型的值，如果属性是引用类型，拷贝的就是内存地址 ，所以如果其中一个对象改变了这个地址，就会影响到另一个对象。
  - 实现:
    - Object.assign() 
        - 将一个或多个源对象的所有**可枚举**属性复制到目标对象，并返回修改后的目标对象，常用于对象的浅拷贝或属性合并。
    - 展开运算符(...)
        - 将一个对象的属性展开为多个参数，常用于对象的浅拷贝或属性合并。
    - Array.prototype.concat()
        - 将一个或多个数组连接成一个新数组，常用于数组的浅拷贝。
    - Array.prototype.slice()
        - 返回一个新数组，包含从原数组中选定的元素，常用于数组的浅拷贝。
    
- 深拷贝
  - 深拷贝是将一个对象从内存中完整的拷贝一份出来,从堆内存中开辟一个新的区域存放新对象,且修改新对象不会影响原对象。
  - 实现:
    - `JSON.parse(JSON.stringify())`
       - **优点**：简单易用。
       - **缺点**：会忽略 `undefined`、`Symbol` 属性，不能序列化函数，不能处理循环引用（会报错），`Date` 对象会变成字符串，`RegExp`、`Map`、`Set` 等特殊对象会变为空对象 `{}`。
    - **递归实现**
       - 这是最根本的实现方式，通过遍历对象、数组，直到里边都是基本数据类型，然后再去复制。可以解决 `JSON` 方法的各种缺陷，但实现起来相对复杂。
    - **第三方库**
       - 在实际项目中，为了保证健壮性，通常会直接使用成熟的第三方库，如 Lodash 的 `_.cloneDeep()` 方法。

**git 联调**
- 入职 git clone 公司代码
      - 主分支 main/master
           所有人都在用的，线上分支
      - 新开一个分支
      在自己的工作任务分支
      git checkout -b xxx
      git branch
      git checkout main
- 常用命令
      git pull origin main 每天上班前的动作
      git status 当前git 状态
      git log --oneline 查看提交记录
      git add . 提交到暂存区
      git commit -m ''提交到仓库
      git push origin main 提交到远程仓库
- 场景
   - 回退
    git restore --staged algorithm/readme.md

**实现流式输出(sse)**
- **前端**
  - 使用 `EventSource` API 连接到后端端点。
  - 监听 `onmessage` 事件来接收服务器推送的数据。
  - 可以监听 `onerror` 处理连接错误，`EventSource` 支持自动重连。
- **后端**
  - **设置响应头**：必须设置 `Content-Type` 为 `text/event-stream`，并建议设置 `Cache-Control: no-cache` 和 `Connection: keep-alive`。
  - **数据格式**：响应体必须遵循特定格式。每条消息以 `data:` 开头，以 `\n\n` 结尾。可以包含 `id`、`event`、`retry` 等字段。
  - **保持连接**：服务器需要保持连接打开，以便随时向客户端推送消息。


**React和vue的区别**

- 设计理念
- **React**：视图库，专注于UI层，需要配合其他库构建完整应用
- **Vue**：渐进式框架，提供完整的解决方案，可以逐步采用
- 数据流
- **React**：单向数据流，数据从父组件流向子组件，通过props传递
- **Vue**：双向数据绑定，支持v-model等指令实现双向数据流
- 模板语法
- **React**：JSX语法，JavaScript和HTML混合编写
- **Vue**：模板语法，HTML增强，指令系统
- 状态管理
- **React**：useState、useReducer等Hooks，需要手动管理状态更新
- **Vue**：响应式系统，自动追踪依赖，数据变化自动更新视图
- 组件通信
- **React**：props向下传递，回调函数向上传递，Context跨层级
- **Vue**：props向下传递，emit向上传递，provide/inject跨层级
- 生命周期
- **React**：useEffect替代生命周期，函数式组件为主
- **Vue**：完整的生命周期钩子，选项式API和组合式API并存
- 学习曲线
- **React**：需要理解函数式编程、Hooks概念，学习曲线较陡
- **Vue**：模板语法直观，渐进式学习，上手较快
- 生态系统
- **React**：Facebook维护，生态丰富，社区活跃
- **Vue**：尤雨溪维护，官方工具链完善，中文文档友好
- 性能优化
- **React**：需要手动优化（React.memo、useMemo、useCallback）
- **Vue**：响应式系统自动优化，内置性能优化机制
- 适用场景
- **React**：大型应用、复杂交互、需要高度定制化
- **Vue**：中小型应用、快速开发、团队协作友好


**介绍一下服务端组件和客户端组件使用场景**
- 服务端组件（Server Components）
  - 特点：
    - 在服务器上渲染，生成 HTML 后发送到客户端（SSR）。
    - 不支持客户端交互（如 useState）。
  - 使用场景：
    - 静态内容展示（如文章列表、数据表格）。
    - 需要 SEO 优化的页面（搜索引擎可直接抓取 HTML）。
    - 减少客户端包体积（布局部分用服务端组件，交互部分用客户端组件）。
- 客户端组件（Client Components）
  - 特点：
    - 在浏览器中执行，支持交互（如 useState、事件监听）。
    - 需要显式标记 use client（Next.js 13+）。
  - 使用场景：
    - 需要动态交互的 UI（如表单验证、动画）。
    - 数据驱动的组件（通过 API 获取数据）。

**ajax请求**
- AJAX（Asynchronous JavaScript and XML）是一种在不重新加载整个页面的情况下，与服务器交换数据并更新部分网页内容的技术。虽然名字里有 XML，但现在最常用的数据格式是 JSON。
- fetch 是基于 Promise 的现代 API，语法更简洁，是当前推荐的标准方式。

**async await底层实现原理**
- 本质上是 `Generator` 函数和 `Promise` 的语法糖，让异步代码的写法更像同步代码。
- `async` 函数会隐式地返回一个 `Promise` 对象。
- `await` 关键字会暂停 `async` 函数的执行，等待后面的 `Promise` 对象状态变为 `fulfilled`，然后将 `Promise` 的结果返回，并继续执行函数。如果 `Promise` 状态变为 `rejected`，它会抛出错误。
- 这种暂停和恢复的机制是通过 `Generator` 函数的协程能力实现的。

**async await 和 promise 是怎么解决回调地狱的**
- **Promise**：通过链式调用 `.then()` 方法，将原本层层嵌套的回调函数，变成一个扁平的、线性的序列。这使得异步操作的流程更加清晰，易于阅读和维护。
- **async/await**：以更彻底的方式解决了回调地獄。它允许我们用写同步代码的方式来编写异步逻辑，完全消除了 `.then()` 链式调用和回调函数。代码的可读性、简洁性和逻辑清晰度都得到了极大的提升。

**事件的循环机制**
- JavaScript是单线程的，为了处理异步任务而不会阻塞主线程，引入了事件循环机制。
- 整体流程是：
  1. 所有同步任务在主线程上执行，形成一个执行栈。
  2. 遇到异步任务时，会将其交给对应的Web API（如浏览器内核或Node.js的API）处理。
  3. 当异步任务完成后（例如定时器到时、网络请求成功），其回调函数会被放入一个任务队列中。
  4. 当主线程的执行栈为空时，事件循环会从任务队列中取出一个任务，压入执行栈中执行。
  5. 这个过程不断重复，就形成了事件循环。

**宏任务和微任务有哪些**
- **宏任务 (Macrotask)**：每次事件循环只会执行一个。它们是较大、独立的任务块。常见的宏任务有：`setTimeout`、`setInterval`、`setImmediate` (Node.js环境)、I/O操作、UI渲染。
- **微任务 (Microtask)**：在当前宏任务执行结束后、下一个宏任务开始前，会一次性执行完队列中所有的微任务。它们通常是需要尽快完成的短小任务。常见的微任务有：`Promise.then/catch/finally`、`process.nextTick` (Node.js环境)、`MutationObserver`。
- **执行顺序**：执行一个宏任务 -> 执行所有微任务 -> 浏览器进行UI渲染 -> 执行下一个宏任务。

**cookie sessionStorage localStorage 的区别**
- **生命周期**：
  - `cookie`：可以设置过期时间，关闭浏览器后不清空（除非设置为会话 `cookie`）。
  - `localStorage`：永久存储，除非手动删除，否则一直存在。
  - `sessionStorage`：仅在当前会话期间有效，关闭浏览器标签页或窗口后数据被清除。
- **存储大小**：
  - `cookie`：非常小，大约4KB。
  - `localStorage` 和 `sessionStorage`：较大，通常为5-10MB。
- **与服务器交互**：
  - `cookie`：每次HTTP请求都会自动携带在请求头中，会增加请求的体积。
  - `localStorage` 和 `sessionStorage`：仅在客户端本地存储，不会自动发送给服务器。

**map和forEach的区别**
- **返回值**：
  - `map`：返回一个**新数组**，这个新数组的每个元素是原数组元素经过处理后的结果。它不会改变原数组。
  - `forEach`：没有返回值，返回 `undefined`。它只是用来遍历数组，对每个元素执行一次操作。
- **用途**：
  - 当你需要根据一个数组生成另一个新的、相关联的数组时，使用 `map`。
  - 当你只需要遍历数组并执行某些操作（例如打印、修改DOM等），而不需要一个新的数组时，使用 `forEach`。
- `map` 支持链式调用其他数组方法（如 `filter`, `reduce`），而 `forEach` 不支持。

**路由的 Hash 模式和 History 模式**
- 这是单页面应用中两种主要的路由模式：
- **Hash 模式**：
  - **URL形态**：带有 `#`，例如 `example.com/#/user`。
  - **原理**：基于URL的哈希值（`location.hash`）变化。哈希值的改变不会触发浏览器向服务器发送请求。
  - **优点**：兼容性好，无需后端配置。
  - **缺点**：URL中带有 `#`，不够美观。
- **History 模式**：
  - **URL形态**：不带 `#`，与普通URL一样，例如 `example.com/user`。
  - **原理**：利用HTML5 History API (`pushState`, `replaceState`) 来改变URL。
  - **优点**：URL更美观，更符合用户习惯。
  - **缺点**：需要后端支持。当用户直接访问或刷新一个深层链接时，服务器需要配置将所有请求都指向应用的入口文件（如`index.html`），否则会报404错误。

**怎么实现jwt鉴权**
1.  **用户登录**：用户使用账号密码登录。
2.  **服务器验证**：服务器验证用户信息是否正确。
3.  **生成Token**：验证通过后，服务器会根据用户信息和预设的密钥，生成一个加密的字符串，即JWT（JSON Web Token）。
4.  **返回Token**：服务器将这个JWT返回给客户端。
5.  **客户端存储**：客户端收到JWT后，通常会将其存储在 `localStorage` 或请求头中。
6.  **后续请求**：客户端在之后每次访问需要权限的接口时，都会在请求头中携带这个JWT。
7.  **服务器鉴权**：服务器收到请求后，会验证请求头中的JWT是否有效（例如，是否被篡改、是否过期）。如果验证通过，则处理请求并返回数据。

**如何实现路由懒加载**
- 路由懒加载也叫动态导入，核心思想是：当用户访问某个路由时，才去加载对应的组件代码，而不是在应用初始加载时就加载所有页面的代码。
- 这样做可以显著减小首次加载的打包文件体积，加快首页的渲染速度，提升用户体验。
- 在Vue或React中，通常结合动态 `import()` 语法和路由配置来实现。当路由被触发时，动态导入的承诺会解决，相应的组件代码块才会被网络请求加载并渲染。

**如何解决跨域问题**
- 跨域是浏览器的一种安全策略（同源策略），限制了从一个源加载的文档或脚本如何与来自另一个源的资源进行交互。
- 常用解决方案：
  1. **CORS (跨源资源共享)**：这是最常用和标准的解决方案。需要后端服务器在响应头中设置特定的字段（如 `Access-Control-Allow-Origin`），明确允许哪些源可以访问其资源。
  2. **JSONP**：一种较老的技术，利用 `script` 标签不受同源策略限制的特点来实现。只支持GET请求，且安全性较低，现在已不常用。
  3. **代理服务器 (Proxy)**：在开发环境中，可以通过配置一个代理服务器来解决。前端的请求都发给同源的代理服务器，再由代理服务器转发到目标API服务器，从而绕过浏览器的跨域限制。Nginx反向代理也是类似原理。

**如何实现图片懒加载**
- 这是一种网页性能优化技术，目的是延迟加载页面上非可视区域的图片，直到用户滚动到它们的位置。
- 实现原理是：
  1. 页面加载时，不将图片的真实URL放在 `img` 标签的 `src` 属性中，而是放在一个自定义属性里（如 `data-src`）。`src` 可以设置一个很小的占位图。
  2. 监听页面的滚动事件，或者使用 `Intersection Observer API` 来监测图片元素是否进入了可视区域。
  3. 当图片进入可视区域时，就将 `data-src` 里的真实URL赋值给 `src` 属性，此时浏览器才会去加载这张图片。
- 这样做可以减少页面的初始加载时间，并节省用户的带宽。

**什么是媒体查询**
- 媒体查询是CSS3的核心功能之一，是实现响应式网页设计的基础。
- 它允许开发者根据设备的特定特征（如视口宽度、高度、屏幕方向、分辨率等）来应用不同的CSS样式。
- 通过媒体查询，我们可以为不同尺寸的设备（如手机、平板、桌面电脑）创建定制化的布局和样式，从而在各种设备上都能提供良好的用户体验。

**如何封装组件**
- 封装组件是为了提高代码的复用性、可维护性和可读性。
- 关键原则包括：
  1. **单一职责原则**：一个组件只做一件事，并把它做好。
  2. **定义清晰的接口 (Props)**：组件通过 `props` 接收外部数据，`props` 的设计应该清晰、易于理解。
  3. **管理内部状态 (State)**：组件应该维护好自己的内部状态，不应轻易暴露给外部。
  4. **提供插槽或组合能力 (Slots/Composition)**：为了提高灵活性，可以预留内容分发的位置（插槽），让使用者可以自定义组件的部分内容。
  5. **样式隔离**：确保组件的样式不会泄露出去影响其他组件，也不会被外部样式轻易污染。

**如何封装axions**
- 封装Axios是为了在项目中统一处理HTTP请求，便于管理。
- 主要步骤：
  1. **创建实例**：使用 `axios.create()` 创建一个Axios实例，并设置基础配置，如基础URL (`baseURL`) 和超时时间 (`timeout`)。
  2. **请求拦截器**：设置请求拦截器，在请求发送前进行统一处理。例如，为每个请求统一添加认证 `token` 到请求头中，或者显示一个加载中的提示。
  3. **响应拦截器**：设置响应拦截器，在接收到响应后进行统一处理。例如，可以预处理返回的数据结构，或者根据不同的HTTP状态码进行统一的错误处理和用户提示。
  4. **API模块化**：将不同业务模块的API请求封装成独立的函数，放在不同的文件中进行管理，使得API调用更加清晰。


**var let const 的区别**
- **作用域**：
  - `var`：函数作用域。在函数外部声明是全局变量。
  - `let` 和 `const`：块级作用域。变量只在声明所在的 `{}` 代码块内有效。
- **变量提升 (Hoisting)**：
  - `var`：存在变量提升，即变量可以在声明前使用，值为 `undefined`。
  - `let` 和 `const`：存在“暂时性死区”（TDZ），即在声明前访问变量会报错。
- **重复声明**：
  - `var`：在同一作用域内可以重复声明同一个变量。
  - `let` 和 `const`：在同一作用域内不可以重复声明。
- **重新赋值**：
  - `var` 和 `let`：声明的变量可以被重新赋值。
  - `const`：声明的是一个只读的常量，一旦声明，其值就不能再改变。对于引用类型（如对象、数组），`const` 保证的是变量指向的内存地址不变，但对象或数组内部的属性或元素是可以修改的。


**如何进行前后端联调**
- 前后端联调是确保前端界面与后端接口正确对接的关键环节。
- **接口文档确认**：首先确认后端提供的接口文档（如 Swagger），包括请求方法、URL、参数格式、响应数据结构等。确保前后端对接口规范的理解一致。
- **Mock 数据先行**：在后端接口未完成时，前端可以使用 Mock 数据（如 Mock.js、JSON Server）模拟接口响应，先完成前端页面的开发和测试。
- **环境与代理配置**：配置开发环境的代理设置（如 Vite/Webpack 的 proxy），解决跨域问题。
- **接口工具测试**：使用 Postman、Apifox 等工具独立测试后端接口，验证其可用性和数据格式。
- **联调与问题排查**：进行联调测试，重点关注数据格式、错误处理、边界情况。遇到问题时，通过浏览器开发者工具的网络和控制台面板，与后端沟通排查。

**如何实现骨架屏**
- 骨架屏是一种加载状态优化技术，在页面内容加载完成前显示页面的基本结构轮廓，提升用户体验。
- **CSS 实现方式**：
  - 使用 `background: linear-gradient()` 创建渐变背景，模拟加载动画效果
  - 通过 `@keyframes` 定义动画，让骨架屏有呼吸灯效果
  - 使用 `border-radius` 和 `margin` 控制骨架屏的形状和间距
- **组件化实现**：
  - 创建可复用的骨架屏组件，支持不同尺寸和形状
  - 通过 `props` 控制骨架屏的宽度、高度、圆角等属性
  - 支持列表、卡片、头像等多种布局模式
- **自动生成**：
  - 使用工具（如 `react-skeleton`、`vue-skeleton-webpack-plugin`）自动生成骨架屏
  - 根据页面结构自动创建对应的骨架屏组件
- **显示控制**：
  - 在数据加载状态为 `loading` 时显示骨架屏
  - 数据加载完成后隐藏骨架屏，显示真实内容
  - 可以通过 `v-if`、`v-show` 或条件渲染控制显示隐藏
- **性能优化**：
  - 骨架屏组件应该轻量化，避免复杂的计算和渲染
  - 使用 `CSS` 动画而非 `JavaScript` 动画，提升性能
  - 考虑使用 `will-change` 属性优化动画性能

**数组的方法**
- `map`：创建一个新数组，其结果是该数组中的每个元素都调用一个提供的函数后返回的结果。
- `forEach`：对数组中的每个元素执行一次提供的函数。
- `filter`：创建一个新数组，其包含通过所提供函数实现的测试的所有元素。
- `reduce`：对数组中的每个元素执行一个由您提供的reducer函数(升序执行)，将其结果汇总为单个返回值。
- `reduceRight`：对数组中的每个元素执行一个由您提供的reducer函数(降序执行)，将其结果汇总为单个返回值。
- `find`：返回数组中满足提供的测试函数的第一个元素的值。否则返回 `undefined`。
- `findIndex`：返回数组中满足提供的测试函数的第一个元素的索引。否则返回 `-1`。
- `some`：测试数组中是否至少有一个元素通过了由提供的函数实现的测试。
- `every`：测试数组中的所有元素是否都通过了由提供的函数实现的测试。
- `fill`：用一个固定值填充数组。
- `reverse`：反转数组。
- `sort`：排序数组。
- `splice`：删除或替换数组中的元素。
- `slice`：返回数组的一部分。
- `any`

**单例模式**
- **定义**：保证一个类仅有一个实例，并提供一个访问它的全局访问点。
- **实现方式**：使用一个变量来标志当前是否已经为某个类创建过对象，如果是，则在下一次获取该类的实例时，直接返回之前创建的对象。
- **使用场景**：
  - **全局状态管理**：如 Vuex 的 Store，Redux 的 Store，确保全局只有一个状态管理器。
  - **登录框/弹窗**：确保页面上只有一个登录框或弹窗实例。
  - **缓存**：在应用中创建一个全局缓存，所有需要缓存的数据都从这个实例中获取。

**web worker**
- **定义**：Web Worker 是一个运行在后台的 JavaScript，独立于其他脚本，不会影响页面的性能。
- **作用**：
  - 在主线程之外创建一个新的工作线程，用于执行耗时的计算或 I/O 操作。
  - 避免因复杂计算导致页面卡顿或无响应，提升用户体验。
- **通信**：主线程和 Worker 线程之间通过 `postMessage()` 方法发送消息，并通过 `onmessage` 事件监听器接收消息。
- **限制**：
  - Worker 线程不能直接访问 `window`、`document` 等 DOM 对象。
  - 只能访问 `navigator`、`XMLHttpRequest` 等部分浏览器 API。
- **使用场景**：
  - **大数据处理**：如图形处理、数据加密解密、大量数据排序等。
  - **后台任务**：如轮询服务器获取最新数据、预加载数据等。

**spa和SSR的区别**
- **SPA (Single-Page Application - 单页应用)**
  - **渲染方式**：客户端渲染 (CSR)。服务器只返回一个空的 HTML 壳和 JS 文件，由浏览器执行 JS 来生成页面内容。
  - **优点**：
    - 页面切换快，用户体验好，接近原生应用。
    - 服务器压力小，只负责提供数据接口。
  - **缺点**：
    - **首屏加载慢**：需要加载完所有 JS 文件才能渲染页面。
    - **SEO 不友好**：搜索引擎爬虫难以抓取由 JS 动态生成的内容。
- **SSR (Server-Side Rendering - 服务器端渲染)**
  - **渲染方式**：服务器端渲染。服务器直接生成完整的 HTML 内容并返回给浏览器。
  - **优点**：
    - **首屏加载快**：浏览器接收到的是完整的 HTML，可以直接渲染。
    - **SEO 友好**：搜索引擎可以直接抓取到完整的页面内容。
  - **缺点**：
    - 服务器压力大，需要为每个请求渲染页面。
    - 页面切换体验不如 SPA 流畅（需要重新请求整个页面）。

**js的数据类型**
- **基本数据类型 (Primitive Types)** - 7种
  - `String`：字符串
  - `Number`：数字
  - `Boolean`：布尔值 (`true`/`false`)
  - `null`：空值
  - `undefined`：未定义
  - `Symbol` (ES6)：唯一的、不可变的值，常用于对象属性名。
  - `BigInt` (ES2020)：用于表示任意精度的整数。
- **引用数据类型 (Reference Types)**
  - `Object`：对象（包括 `Array` 数组、`Function` 函数、`Date` 日期等）。

**seo**
- **定义**：SEO (Search Engine Optimization) 即搜索引擎优化，指通过优化网站结构和内容，使其在搜索引擎的搜索结果中获得更好的排名。
- **前端优化策略**：
  - **语义化 HTML**：使用正确的 HTML 标签（如 `<header>`, `<main>`, `<h1>-<h6>`）来组织内容，帮助搜索引擎理解页面结构。
  - **Meta 标签优化**：
    - `<title>`：设置有吸引力且包含关键词的标题。
    - `<meta name="description">`：提供页面的简要描述。
    - `<meta name="keywords">`：设置页面关键词。
  - **URL 结构**：使用清晰、简洁且具有描述性的 URL。
  - **图片优化**：为 `<img>` 标签添加 `alt` 属性，描述图片内容。
  - **网站性能**：提升网站加载速度（如压缩图片、代码，使用缓存），因为网站性能是谷歌排名的重要因素之一。
  - **SSR/SSG**：采用服务器端渲染或静态站点生成，确保搜索引擎能抓取到完整的页面内容。

**生命周期**
- React组件的生命周期指的是一个组件从被创建、挂载到DOM，经历数据更新，最后被卸载和销毁的整个过程。在这个过程中，React为我们提供了一系列的生命周期方法（Lifecycle Methods），允许我们在特定的时机执行自定义的逻辑，比如发起网络请求、设置定时器、更新状态、清理资源等。
- React的生命周期在类组件（Class Components）中体现得最为清晰和完整。随着React 16.8引入Hooks，函数组件也能通过useEffect等Hook来模拟类似生命周期的行为，但理解类组件的生命周期是理解React工作原理的基础。
- 生命周期大致分为三个主要阶段:
   - 初始化阶段（Mounting）：组件被创建，挂载到DOM，并初始化状态和属性。
      - constructor(props):组件初始化时调用，用于初始化state和绑定事件处理函数的this上下文
      - static getDerivedStateFromProps(props, state)
      - render():组件渲染函数，返回JSX
      - componentDidMount():组件挂载到DOM后调用，用于执行一些初始化操作
   - 更新阶段（Updating）：组件在数据更新时，重新渲染。
      - componentWillReceiveProps(nextProps):组件接收到新的props时调用
      - shouldComponentUpdate(nextProps, nextState):组件接收到新的props或state时调用，返回一个布尔值，决定是否重新渲染组件
      - componentWillUpdate(nextProps, nextState):组件重新渲染前调用
      - render():组件渲染函数，返回JSX
      - componentDidUpdate(prevProps, prevState):组件重新渲染后调用
   - 卸载阶段（Unmounting）：组件被从DOM中移除，释放资源。
      - componentWillUnmount():组件卸载前调用，用于执行一些清理操作
- 在React 16.8引入Hooks之前，类组件的生命周期是固定的，而Hooks允许我们在函数组件中使用类似生命周期的逻辑，使得函数组件也能管理状态和副作用。
- 理解生命周期有助于我们编写更高效、更健壮的React组件。

- useEffect(() => { ... }, []) 模拟 componentDidMount（空依赖数组）。
- useEffect(() => { ... }) 模拟 componentDidUpdate（无依赖数组，每次渲染后）或特定props/state变化时执行（依赖数组指定）。
- useEffect 的返回函数模拟 componentWillUnmount，用于清理。
- useLayoutEffect 在DOM更新后同步执行，类似getSnapshotBeforeUpdate后的操作。



**css优先级**
- CSS 优先级（Specificity）是浏览器用来决定当多个 CSS 规则应用于同一个元素时，哪一个规则的样式最终生效的机制。简单来说，就是“谁的规则更具体，谁就赢”。
- !important 声明
  - 这是最高优先级。在 CSS 声明末尾加上 !important，可以强制覆盖其他所有优先级的规则
  - !important 应谨慎使用，因为它会破坏层叠（Cascading）的自然流程，使样式难以维护和调试
- 内联样式 (Inline Styles)
  - 直接写在 HTML 元素 style 属性中的 CSS 规则。
  - 优先级高于外部样式表和内部样式表中的规则。
- 选择器的特异性 (Selector Specificity)
- 特异性通常用一个四元组 (a, b, c, d) 来表示，从左到右权重递减
  a: 内联样式的数量。这里通常为 0 或 1（1 表示内联样式本身，但内联样式的优先级已在上一步单独考虑，所以计算选择器特异性时 a 通常为 0）。
  b: ID 选择器的数量。
  c: 类选择器 (class)、属性选择器 (attribute) 和伪类 (pseudo-classes) 的数量。
  d: 元素选择器 (element) 和伪元素 (pseudo-elements) 的数量。

**移动端适配**
- 关于lib-flexible + postcss-pxtorem方案：
    - lib-flexible是阿里团队开发的一个移动端适配库，它的核心思想是动态设置html根元素的font-size，实现不同屏幕下的等比缩放。
    - 具体工作原理是：lib-flexible会根据设备的屏幕宽度，动态计算并设置html的font-size值。通常是将屏幕宽度分为10等份，比如375px宽的屏幕，html的font-size就设置为37.5px。
    - 然后配合postcss-pxtorem这个插件，它能够在构建阶段自动将我们写的px单位转换为rem单位。比如我们写16px，插件会自动转换为对应的rem值。
    - 这样的好处是：开发时我们还是按照设计稿的px值来写，但最终会转换成适配性更好的rem单位。
- 关于postcss-px-to-viewport方案：
    - 这是一个更现代的解决方案，直接将px转换为vw/vh单位。工作原理是根据设计稿尺寸，计算出px到vw的转换比例。
    - 比如设计稿宽度是375px，那么1px就等于100/375vw，插件会自动帮我们做这个转换。
- 两种方案的对比：
    - lib-flexible方案的优势是兼容性好，在一些老旧设备上表现稳定，而且可以通过JavaScript灵活控制。
    - postcss-px-to-viewport方案更简洁，不需要引入额外的JavaScript库，纯CSS解决，性能更好。而且vw单位是CSS原生支持的，更符合标准。
    - 但vw方案在一些特殊场景下可能需要额外处理，比如需要设置最大宽度限制，或者处理横屏情况。
- 实际使用经验：
    - 在我的项目经验中，现在更倾向于使用vw方案，因为现代浏览器对vw的支持已经很好了，而且代码更简洁。不过对于需要兼容老版本iOS设备的项目，我还是会选择lib-flexible方案，因为它的兼容性更可靠。
- **rem,vw**
    - 关于rem：
        - rem是"root em"的缩写，它是相对于根元素html的font-size来计算的。比如说，如果html的font-size是16px，那么1rem就等于16px，2rem就是32px。
        - rem的主要优势是可以实现整体的缩放效果。当我们改变根元素的font-size时，所有使用rem单位的元素都会按比例缩放，这对于响应式设计和适配不同屏幕尺寸非常有用。
    - 在实际项目中，我们通常会设置html的font-size为62.5%，也就是10px，这样1rem就等于10px，方便我们进行计算和转换。
    - 关于vw：
        - vw是"viewport width"的缩写，表示视口宽度的百分比。1vw等于视口宽度的1%，100vw就是整个视口的宽度。
        - vw的优势在于它能够根据屏幕宽度动态调整，特别适合做移动端适配。比如我们可以用vw来设置字体大小，实现文字随屏幕宽度自动缩放的效果。
    - 两者的区别：
        - 主要区别在于参照标准不同：rem参照的是根元素字体大小，而vw参照的是视口宽度。rem更多用于整体布局的缩放控制，vw更多用于响应式设计中的尺寸适配。
        - 在移动端开发中，我经常结合使用这两个单位，比如用rem做整体布局，用vw做字体和间距的适配，这样可以获得更好的响应式效果。

**css盒模型**
- CSS盒模型是CSS布局的核心概念，它定义了HTML元素在页面上如何被渲染和占据空间。简单来说，每一个HTML元素都可以被看作是一个矩形的“盒子”。这个“盒子”由四个部分组成，由内到外分别是：
    - 内容（Content）:
        - 这是盒子最核心的部分，用于放置文本、图片或其他内容。
        - 可以通过 width 和 height 属性来设置其尺寸。
    - 内边距（Padding）:
        - 内容与边框之间的透明区域。
        - 背景色或背景图像会延伸到内边距区域。
        - 使用 padding 属性设置（可分别设置上、右、下、左 padding-top, padding-right 等）。
    - 边框（Border）:
        - 环绕在内边距和内容周围的线条。
        - 可以设置边框的样式（solid, dashed等）、宽度（border-width）和颜色（border-color）。
        - 使用 border 属性或其简写形式设置。
    - 外边距（Margin）:
        - 盒子与其他盒子之间的透明区域，用于控制元素之间的间距。
        - 外边距是完全透明的，不会显示背景。
        - 使用 margin 属性设置（可分别设置上、右、下、左 margin-top, margin-right 等）。
    - 元素的总占用空间计算：
        - 一个元素在页面上实际占据的总宽度和总高度，是这四个部分之和：

        - 总宽度 = width + padding-left + padding-right + border-left-width + border-right-width + margin-left + margin-right
        - 总高度 = height + padding-top + padding-bottom + border-top-width + border-bottom-width + margin-top + margin-bottom
    - 盒模型的两种类型
        - CSS中存在两种盒模型，它们决定了width和height属性具体包含哪些部分：

        - 标准盒模型（W3C Box Model） - 默认
            - 在这种模型下，我们设置的 width 和 height 仅指内容区域的尺寸。
            - 内边距、边框和外边距是在这个尺寸之外额外增加的。
            - 这是浏览器的默认行为。
            - 计算方式：box-sizing: content-box; (这是默认值)
        - IE盒模型（或替代盒模型）
            - 在这种模型下，我们设置的 width 和 height 包含了内容、内边距和边框。
            - 内边距和边框的尺寸会从内容区域中“扣除”，而不会增加元素的总尺寸。
            - 这种模型最初由旧版IE浏览器采用，但现在通过CSS3的 box-sizing 属性可以明确指定。
            - 计算方式：box-sizing: border-box;
- CSS盒模型是每个元素都遵循的矩形盒子结构，由 内容、内边距、边框、外边距 四部分构成。
- 元素的总占用空间是这四部分尺寸的总和。
- 存在两种盒模型：标准盒模型（content-box，width/height 仅指内容）和 IE盒模型（border-box，width/height 包含内容、padding和border）。
- 通过 box-sizing 属性可以切换盒模型，强烈推荐在项目中使用 box-sizing: border-box，因为它能让布局计算更直观、更可靠。

- **浏览器进程**
当用户在浏览器地址栏输入一个URL并按下回车后，浏览器会经历一系列复杂的步骤来获取并渲染页面。这个过程可以分为以下几个阶段：

1. **URL解析**：
   - 浏览器首先解析URL，确定要访问的资源类型（HTTP、HTTPS、FTP等）。
   - 如果URL包含域名，浏览器会进行DNS解析，将域名转换为IP地址。

2. **建立连接**：
   - 浏览器与服务器建立连接。
   - 如果使用HTTPS，浏览器会进行TLS握手，确保数据传输的安全性。
3. **发送请求**：
   - 浏览器发送HTTP请求，请求头包含URL、方法（GET、POST等）、请求体等。
4. **服务器响应**：
   - 服务器处理请求，返回响应头和响应体。
5. **渲染页面**：
   - 浏览器解析响应体，生成DOM树。
   - 解析CSS，生成CSSOM树。
   - 合并DOM树和CSSOM树，生成渲染树。
   - 布局渲染树，计算每个元素的位置和大小。
6. **显示页面**：
   - 浏览器将渲染树绘制到屏幕上，显示给用户。
7. **资源加载**：
   - 浏览器解析HTML时，会遇到各种资源（图片、CSS、JS等），并发送请求获取这些资源。
8. **事件处理**：
   - 用户与页面交互时，浏览器会处理相应的事件（点击、滚动、输入等）。
9. **垃圾回收**：
   - 浏览器会定期清理不再使用的内存，释放资源。

- 解析URL -> DNS查询 -> 建立TCP连接（+TLS握手）-> 发送HTTP请求 -> 服务器处理并返回响应 -> 浏览器解析HTML/CSS/JS -> 构建DOM/CSSOM -> 生成渲染树 -> 布局 -> 绘制 -> 合成 -> 显示页面。